# Leave blank for same-origin deployments. Vite uses this only for browser API requests.
VITE_API_BASE_URL=

# Development-server proxy target. Change only when the API listens elsewhere.
VITE_API_PROXY_TARGET=http://localhost:5230