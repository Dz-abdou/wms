import { Alert, Button, Descriptions, Popconfirm, Space, Spin, Typography } from 'antd'
import { Link, useParams } from 'react-router-dom'
import { useTranslation } from 'react-i18next'
import { getErrorMessage } from '../../../shared/errors/problemDetails'
import { formatDateTime } from '../../../shared/formatting/dateTime'
import { toAppLanguage } from '../../../shared/i18n/constants'
import { ProductStatusTag } from '../../products/components/ProductStatusTag'
import { useSetWarehouseStatus, useWarehouse } from '../api/useWarehouses'
import { warehouseRoutes } from '../warehouseConstants'
export function WarehouseDetailPage(){const {id}=useParams();const {i18n,t}=useTranslation();const q=useWarehouse(id);const set=useSetWarehouseStatus(id??'');if(q.isLoading)return <Spin className="page-spinner" size="large" tip={t('warehouses.loadingOne')}/>;if(q.error||!q.data||!id)return <Alert message={getErrorMessage(t,q.error,'warehouses.errors.load')} showIcon type="error"/>;const w=q.data;const action=t(w.isActive?'warehouses.deactivate':'warehouses.activate');return <section><div className="page-heading"><Typography.Title level={2}>{w.name}</Typography.Title><Space><Button><Link to={warehouseRoutes.edit(id)}>{t('warehouses.edit')}</Link></Button><Popconfirm cancelText={t('warehouses.cancel')} description={t('warehouses.confirmStatusDescription',{action:action.toLocaleLowerCase(i18n.language)})} okText={action} onConfirm={()=>set.mutateAsync(!w.isActive)} title={t('warehouses.confirmStatusTitle',{action})}><Button danger={w.isActive} loading={set.isPending}>{action}</Button></Popconfirm></Space></div>{set.error?<Alert className="page-alert" message={getErrorMessage(t,set.error,'warehouses.errors.status')} showIcon type="error"/>:null}<Descriptions bordered column={1}><Descriptions.Item label={t('warehouses.table.code')}>{w.code}</Descriptions.Item><Descriptions.Item label={t('warehouses.table.name')}>{w.name}</Descriptions.Item><Descriptions.Item label={t('warehouses.table.description')}>{w.description??t('warehouses.missingDescription')}</Descriptions.Item><Descriptions.Item label={t('warehouses.table.status')}><ProductStatusTag isActive={w.isActive}/></Descriptions.Item><Descriptions.Item label={t('warehouses.table.created')}>{formatDateTime(w.createdAtUtc,toAppLanguage(i18n.resolvedLanguage))}</Descriptions.Item><Descriptions.Item label={t('warehouses.table.updated')}>{formatDateTime(w.updatedAtUtc,toAppLanguage(i18n.resolvedLanguage))}</Descriptions.Item></Descriptions></section>}